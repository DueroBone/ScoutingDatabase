Data structure: // + means another could be many of that item
  Events < eventName+ < Teams < teamNumber+ < liveScrapedInfo // updated every ~30 sec
                                              staticScoutedInfo
                                              roundInfo+ (named)
                        Input < scouterName+ < matchNumber+ < teamNumber+ < data || piped to roundInfo+ after x rounds